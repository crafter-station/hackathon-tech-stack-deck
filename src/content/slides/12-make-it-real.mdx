export const meta = {
  slug: "make-it-real",
  title: "Make It Real",
  theme: "dark",
};

export const notes = [
  "This is where it becomes a product.",
  "Even if it’s simple, it actually does something useful.",
];

export const references = [
  {
    label: "OpenAI docs",
    href: "https://platform.openai.com/docs",
  },
];

<Prose>
  <p className="text-sm uppercase tracking-[0.4em] text-zinc-500">Make it real</p>
  <h1>Add one real behavior:</h1>
  <div className="rounded-[2rem] border border-white/10 bg-white/[0.04] p-6 sm:p-8">
    <p className="text-3xl font-medium tracking-[-0.05em] text-white">generate questions from notes</p>
  </div>
</Prose>
