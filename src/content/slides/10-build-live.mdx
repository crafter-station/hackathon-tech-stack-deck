export const meta = {
  slug: "build-live",
  title: "Build (Live)",
  theme: "dark",
};

export const notes = [
  "Follow along.",
  "If it breaks, that’s fine.",
  "We’re optimizing for speed, not perfection.",
];

<Prose>
  <p className="text-sm uppercase tracking-[0.4em] text-zinc-500">Build live</p>
  <h1>Goal: working interface</h1>
  <ul>
    <li>input (notes)</li>
    <li>output (questions)</li>
    <li>simple layout</li>
  </ul>
</Prose>
