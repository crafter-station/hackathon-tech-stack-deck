export const meta = {
  slug: "publish",
  title: "Publish",
  theme: "dark",
};

export const notes = [
  "Deployment is not optional.",
  "Your app should have a URL.",
  "That’s when the loop becomes real.",
];

<div className="space-y-10">
  <p className="text-sm uppercase tracking-[0.4em] text-zinc-500">Publish</p>
  <Quote author="Workshop rule">If it’s not live, it doesn’t count.</Quote>
</div>
