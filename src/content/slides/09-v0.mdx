export const meta = {
  slug: "v0",
  title: "Tool 1",
  theme: "dark",
};

export const notes = [
  "We’ll use v0 to go from idea to interface.",
  "Don’t overthink it. Prompt, adjust, repeat.",
  "The goal is not perfection. The goal is something usable.",
];

export const references = [
  {
    label: "v0",
    href: "https://v0.dev/",
  },
];

<Prose>
  <p className="text-sm uppercase tracking-[0.4em] text-zinc-500">Tool 1</p>
  <div className="inline-flex rounded-full border border-white/10 bg-white/[0.04] px-4 py-2 text-xs uppercase tracking-[0.28em] text-zinc-400">
    Fastest way to shape an idea
  </div>
  <h1>v0</h1>
  <div className="rounded-[2rem] border border-white/10 bg-[linear-gradient(180deg,rgba(255,255,255,0.08),rgba(255,255,255,0.03))] p-6 sm:p-8">
    <p className="text-base text-zinc-300">Prompt → interface → adjust → repeat</p>
  </div>
</Prose>
